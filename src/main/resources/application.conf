client {
  initial-query = "What are the main differences between functional and object-oriented programming?"  # Your initial query here
}

bedrock-server {
  url = "http://localhost:8080"  # Server endpoint
}

ollama {
  host = "http://localhost:11434"  # Ollama API endpoint
  model = "llama3.2:latest"  # or whatever model you're using
  max-turns = 10
  request-timeout-seconds = 30
}